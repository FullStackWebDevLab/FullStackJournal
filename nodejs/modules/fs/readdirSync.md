# `readdirSync`

Read the contents of a directory.

```
readdirSync(path[, options])
```

+ `path`: string, Buffer, or URL. The path to the directory to read.
+ Returns: `<string[]>` | `<Buffer[]>` | `<fs.Dirent[]>`

---
