# Glossary

+ **Pure function**. A pure function is a function that returns the same output given the same inputs. In react, a pure function must always return the same JSX given the same inputs (props, state, and context).

---
